FROM node:alpine

WORKDIR /usr/app

COPY package*.json ./

RUN npm install
RUN npm install -g nodemon

COPY ./src .

# RUN echo 'nameserver 8.8.8.8'>/etc/resolv.conf

EXPOSE 8080
CMD [ "npm", "start" ]
